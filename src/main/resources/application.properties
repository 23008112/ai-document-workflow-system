server.port=8080

# ?? MySQL Database ??????????????????????????????????????
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/docflow_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Aravind06#}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? JPA ?????????????????????????????????????????????????
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ?? H2 Console ???????????????????????????????????????????
spring.h2.console.enabled=false

# ?? JWT ??????????????????????????????????????????????????
jwt.secret=${JWT_SECRET:MyVeryVeryVeryVeryVeryVerySecureSecretKey123456}

# ?? File Upload ??????????????????????????????????????????
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB